# Progress Log

## 2026-02-17 - Task: 项目方向迁移（Video → ResourceHub）

### What was done:
- 将项目定位从视频生成平台改为资源分享网站。
- 重写 architecture.md，改为 ResourceHub 架构（资源发布、检索、评论、收藏、下载、审核）。
- 重写 task.json，拆分为 24 个任务并全部标记为 `passes: false`。
- 更新 AGENT.md 工作流，改为 Codex 驱动。
- 更新 README.md，替换 Claude 使用说明为 Codex。
- 更新 run-automation.sh，CLI 从 `claude` 切换到 `codex exec`。
- 调整 init.sh，支持自动识别应用目录并兼容未初始化状态。

### Testing:
- 运行 `bash -n /Users/wxx/Desktop/code/auto-coding-agent-demo/init.sh`
- 运行 `bash -n /Users/wxx/Desktop/code/auto-coding-agent-demo/run-automation.sh`
- 运行 `python3 -m json.tool /Users/wxx/Desktop/code/auto-coding-agent-demo/task.json`

### Notes:
- 当前仓库尚未包含 `resource-hub/` 应用代码，后续任务会逐步创建。
- 若未设置 `APP_DIR`，脚本会自动探测包含 `package.json` 的目录。

## 2026-02-17 - Task: 初始化 Next.js 项目

### What was done:
- 在 `resource-hub/` 初始化 Next.js + TypeScript 项目（App Router + `src` 目录）。
- 确认 TypeScript `strict` 已启用，ESLint 使用 `eslint-config-next`（含 TypeScript 规则）。
- 创建基础目录并纳入版本管理：`src/components`、`src/lib`、`src/types`。
- 调整 `src/app/layout.tsx`，移除 `next/font/google` 远程字体依赖，避免构建阶段网络阻塞。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass

### Notes:
- 初次执行 `./init.sh` 时依赖安装未完成，后续通过联网安装补齐依赖后完成验证。

---

## Template for New Entries

```text
## [Date] - Task: [task title from task.json]

### What was done:
- [specific changes made]

### Testing:
- [commands and results]

### Notes:
- [risks, blockers, follow-up]
```

## 2026-02-17 - Task: 基础依赖与 UI 框架

### What was done:
- 在 `resource-hub/` 安装核心依赖：`tailwindcss`、`clsx`、`tailwind-merge`、`@supabase/supabase-js`、`@supabase/ssr`，并补充 `@tailwindcss/postcss`。
- 新增 `resource-hub/postcss.config.mjs`，启用 Tailwind 的 PostCSS 插件。
- 新增 `resource-hub/src/lib/utils.ts` 并实现 `cn()`，统一处理 className 合并。
- 新增通用容器组件 `resource-hub/src/components/layout/container.tsx`。
- 重构 `resource-hub/src/app/layout.tsx`、`resource-hub/src/app/globals.css`、`resource-hub/src/app/page.tsx`，完成基础布局与全局样式初始化；移除默认模板样式文件 `resource-hub/src/app/page.module.css`。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass
- 使用 Playwright 访问 `http://localhost:3001` 并检查首页结构与关键文案: pass

### Notes:
- 本机 `3000` 端口被占用，Next.js dev server 自动切换到 `3001`，不影响任务交付。

## 2026-02-17 - Task: 环境变量与配置模板

### What was done:
- 新增 `resource-hub/.env.local.example`，提供 Supabase URL、Anon Key、Service Role Key、Storage Bucket 模板变量。
- 新增 `resource-hub/src/lib/env.ts`，统一实现环境变量读取与必填校验，并区分 `publicEnv` 与 `getServerEnv()` 访问方式。
- 更新 `resource-hub/README.md`，补充 `.env.local` 初始化、变量说明与环境校验模块使用说明。
- 更新 `resource-hub/.gitignore`，允许提交 `.env.local.example` 模板文件。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass

### Notes:
- `src/lib/env.ts` 采用 fail-fast 校验，缺失关键变量时会直接抛错，避免运行期隐性配置问题。

## 2026-02-17 - Task: 数据库 Schema 设计与迁移

### What was done:
- 新增迁移文件 `resource-hub/supabase/migrations/20260217182000_create_resourcehub_core_schema.sql`。
- 创建核心表：`categories`、`tags`、`resources`、`resource_tags`、`comments`、`favorites`、`downloads`。
- 为 `resources.status` 使用 `resource_status` enum，并增加 `search_vector` 生成列支持 Full-Text Search。
- 按任务要求补充索引：`status`、`category_id`、`created_at`、`search_vector`，并为关联查询补充常用辅助索引。
- 添加 `set_updated_at` trigger function，并在 `resources`、`comments` 上挂载更新时间触发器。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass

### Notes:
- 当前任务仅完成 schema migration，不包含 RLS policy；后续安全任务可基于该结构继续补齐权限策略。

## 2026-02-17 - Task: Supabase 客户端封装

### What was done:
- 新增 `resource-hub/src/lib/supabase/client.ts`，封装浏览器端 Supabase Client 创建逻辑。
- 新增 `resource-hub/src/lib/supabase/server.ts`，封装服务端 Supabase Client，并接入 `next/headers` cookies 读写。
- 新增 `resource-hub/src/lib/supabase/middleware.ts`，实现 `updateSession()`，通过 `supabase.auth.getUser()` 触发会话刷新。
- 新增 `resource-hub/src/middleware.ts`，接入 `updateSession()` 并配置 matcher，跳过静态资源路径。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass（Next.js 提示 `middleware` 命名约定未来将迁移到 `proxy`，不影响当前构建）

### Notes:
- 当前实现遵循 Supabase SSR 推荐的 cookies 同步方式，可为后续认证与路由保护任务复用。

## 2026-02-24 - Task: 认证模块

### What was done:
- 完成并核对认证模块实现：`/login`、`/register` 页面已接入统一 `AuthForm`，支持 email/password 登录与注册。
- 确认已提供 `LogoutButton` 组件，并在 `AuthNav` 中根据登录态展示发布、Dashboard 与退出按钮。
- 确认 `src/lib/supabase/middleware.ts` 已对 `/submit`、`/dashboard`、`/admin` 路由做登录保护，并处理登录页/注册页的已登录跳转逻辑。
- 本次会话将任务状态更新为通过，并同步进度记录。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass
- 运行本地 smoke test（启动 dev server 后检查 `/login`、`/register` 页面文案与 `/dashboard` 未登录重定向）: pass

### Notes:
- 构建与开发阶段仍有 Next.js `middleware` 命名弃用提示（建议后续迁移到 `proxy`）。

## 2026-02-24 - Task: Storage 上传服务

### What was done:
- 新增 `resource-hub/src/app/api/upload/route.ts`，实现 `POST /api/upload` 上传接口。
- 接口接入 Supabase 登录态校验，未登录请求会返回 `401`。
- 增加上传规则：按 `kind` 区分 `resource` 与 `cover`，分别校验文件大小上限、扩展名白名单与 MIME 白名单。
- 通过 Service Role 客户端上传文件到 Supabase Storage，并按 `userId/kind/timestamp` 组织路径。
- 上传成功后返回可访问 URL（优先 signed URL）和文件元信息（bucket/path/type/size 等）。
- 新增 `resource-hub/src/lib/supabase/admin.ts`，统一封装服务端 Admin Client 创建逻辑。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass

### Notes:
- 仍存在 Next.js `middleware` 命名弃用提示（建议后续迁移到 `proxy`），不影响本任务交付。

## 2026-02-24 - Task: 分类与标签数据层

### What was done:
- 新增 `resource-hub/src/lib/slug.ts`，提供通用 `slugify()`，统一处理空格、大小写、分隔符与非法字符。
- 新增 `resource-hub/src/lib/db/categories.ts`，实现分类查询与创建逻辑，创建时按 slug 去重并处理并发唯一键冲突回退。
- 新增 `resource-hub/src/lib/db/tags.ts`，实现标签列表查询、关键词检索、单个创建与批量创建，包含输入去重与冲突兜底。
- 新增 `resource-hub/src/types/database.ts`，补充 Supabase `Database` 类型定义；更新 `resource-hub/src/lib/supabase/admin.ts` 使用强类型 client。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass

### Notes:
- 构建仍会提示 Next.js `middleware` 命名弃用（建议后续迁移到 `proxy`），不影响当前任务交付。

## 2026-02-24 - Task: 资源数据层

### What was done:
- 新增 `resource-hub/src/lib/db/resources.ts`，实现资源数据层核心能力。
- 实现 `createResource/getResourceById/updateResource/deleteResource`，支持资源基础 CRUD。
- 实现 `listResources`，支持分页、关键字检索（FTS）、分类过滤、标签过滤和排序（最新/最热/最多下载）。
- 实现 `updateResourceStatus`，支持 `pending/published/rejected` 状态切换，并自动维护 `published_at/rejected_at/review_reason` 字段。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass

### Notes:
- 构建仍会提示 Next.js `middleware` 命名弃用（建议后续迁移到 `proxy`），不影响当前任务交付。

## 2026-02-24 - Task: 互动数据层

### What was done:
- 新增 `resource-hub/src/lib/db/comments.ts`，实现评论查询、分页列表、创建、更新、删除等数据访问能力。
- 新增 `resource-hub/src/lib/db/favorites.ts`，实现收藏状态查询、增删切换、用户收藏列表，并补充资源 `favorite_count` 计数回写。
- 新增 `resource-hub/src/lib/db/downloads.ts`，实现下载记录创建、按用户/资源分页查询，并补充资源 `download_count` 计数回写。
- 三个模块统一了输入归一化、分页参数处理与错误包装，保持与现有数据层风格一致。

### Testing:
- 运行 `npx eslint src/lib/db/comments.ts src/lib/db/favorites.ts src/lib/db/downloads.ts`（in `resource-hub/`）: pass
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass

### Notes:
- 构建仍会提示 Next.js `middleware` 命名弃用（建议后续迁移到 `proxy`），不影响当前任务交付。

## 2026-02-24 - Task: 资源 API - 基础 CRUD

### What was done:
- 新增 `resource-hub/src/lib/api/response.ts`，统一 API 成功/失败响应结构（`{ data }` / `{ error: { code, message } }`）。
- 新增 `resource-hub/src/app/api/resources/route.ts`，实现 `GET /api/resources`（分页、关键字、分类、标签、排序）与 `POST /api/resources`（登录态校验 + 创建资源）。
- 新增 `resource-hub/src/app/api/resources/[id]/route.ts`，实现 `GET/PATCH/DELETE /api/resources/:id`，包含资源存在性校验与 owner 权限控制。
- 为查询参数与 JSON body 增加输入校验，统一返回 `400/401/403/404/500` 结构化错误码和错误信息。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass

### Notes:
- `GET /api/resources` 默认仅返回 `published` 资源；当登录用户查询自身资源时支持查看非公开状态。
- 构建仍会提示 Next.js `middleware` 命名弃用（建议后续迁移到 `proxy`），不影响当前任务交付。

## 2026-02-24 - Task: 互动 API

### What was done:
- 新增 `resource-hub/src/app/api/resources/[id]/comments/route.ts`，实现 `GET/POST /api/resources/:id/comments`，支持分页查询与评论创建。
- 新增 `resource-hub/src/app/api/resources/[id]/favorite/route.ts`，实现 `POST /api/resources/:id/favorite`，按用户对资源执行收藏状态切换。
- 新增 `resource-hub/src/app/api/resources/[id]/download/route.ts`，实现 `POST /api/resources/:id/download`，写入下载记录并触发下载计数回写。
- 三个互动 API 统一接入资源存在性与发布状态校验，并在写操作中校验登录态，沿用统一错误响应结构。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass

### Notes:
- 当前 `download` 接口要求登录用户触发下载记录；后续如需支持匿名下载，可在该路由扩展 `ip_hash` 逻辑。
- 构建仍会提示 Next.js `middleware` 命名弃用（建议后续迁移到 `proxy`），不影响当前任务交付。

## 2026-02-24 - Task: 分类标签 API

### What was done:
- 新增 `resource-hub/src/app/api/categories/route.ts`，实现 `GET /api/categories`，返回分类列表并统一错误响应格式。
- 新增 `resource-hub/src/app/api/tags/route.ts`，实现 `GET /api/tags`，支持 `keyword` 关键词查询和 `limit` 条数参数。
- 在分类与标签查询 API 中接入 `next/cache` 的 `unstable_cache`，提供 60 秒的轻量缓存策略，减少重复读查询。
- 标签接口补充 `limit` 参数校验，非法值统一返回 `400 INVALID_LIMIT`。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass

### Notes:
- 构建仍会提示 Next.js `middleware` 命名弃用（建议后续迁移到 `proxy`），不影响当前任务交付。

## 2026-02-24 - Task: 审核 API

### What was done:
- 新增 `resource-hub/src/lib/auth/admin.ts`，实现管理员鉴权工具，严格基于 `app_metadata` 中的 `role/roles/is_admin` 判定管理员身份。
- 新增 `resource-hub/src/app/api/admin/resources/pending/route.ts`，实现 `GET /api/admin/resources/pending`，支持 `page/pageSize` 参数并返回待审核资源分页列表。
- 新增 `resource-hub/src/app/api/admin/resources/[id]/review/route.ts`，实现 `POST /api/admin/resources/:id/review`。
- 审核接口支持 `approve/reject` 动作，`reject` 时强制校验 `reason`，并通过 `updateResourceStatus` 写入资源状态与审核原因。
- 审核接口增加资源状态前置校验，仅允许 `pending` 资源审核，非 `pending` 返回 `409 RESOURCE_NOT_PENDING`。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass

### Notes:
- 构建仍会提示 Next.js `middleware` 命名弃用（建议后续迁移到 `proxy`），不影响当前任务交付。

## 2026-02-25 - Task: 首页 UI

### What was done:
- 完成首页 Hero 区块，补充平台定位文案与双 CTA（`Start Exploring`、`Share a Resource`）。
- 首页新增 `Popular Categories` 区块，接入分类数据读取并在查询失败时提供 fallback 分类。
- 首页新增 `Latest Resources` 区块，接入已发布资源列表，包含发布时间、收藏数、下载数与空状态展示。
- 升级全局 Header：品牌 Logo 可回到首页，桌面端新增主导航（Home/Browse/Publish）并按登录态展示操作入口。
- 完成移动端导航菜单（`details/summary` 方案），支持未登录与已登录两套入口渲染。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass
- 使用 Playwright CLI 访问 `http://localhost:3000`，核验首页 Hero、热门分类、最新资源区块与移动端菜单展开: pass

### Notes:
- `Browse` 相关链接当前指向 `/resources`，完整列表页能力将在后续 Task 16 交付。

## 2026-02-25 - Task: 资源列表页 UI

### What was done:
- 新增 `resource-hub/src/app/resources/page.tsx`，实现 `/resources` 页面主结构与说明区。
- 实现关键字搜索、分类筛选、标签筛选（多选 tag checkbox）与排序（Latest / Most Favorited / Most Downloaded）。
- 实现分页参数与导航（Previous / Next），并保持筛选条件在分页跳转中的查询串同步。
- 实现资源卡片列表展示（标题、描述摘要、发布时间、收藏/下载统计、详情入口）与空状态视图。
- 补充数据层容错：分类/标签/资源查询失败时降级为空数据并显示提示，避免页面直接 500。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass
- 使用 Playwright CLI 访问 `http://localhost:3000/resources`，核验页面元素（Keyword/Sort/Category/Apply filters）: pass
- 使用 Playwright CLI 填写关键词并提交，确认 URL 查询串更新为 `?keyword=design+system&sortBy=latest&category=`，且有筛选空状态与 `Clear filters` 入口: pass

### Notes:
- 本地 Supabase 环境当前缺少部分表时，页面会显示数据不可用提示并降级展示空列表；待迁移执行完成后可恢复完整数据展示。
- 构建仍会提示 Next.js `middleware` 命名弃用（建议后续迁移到 `proxy`），不影响本任务交付。

## 2026-02-25 - Task: 资源详情页 UI

### What was done:
- 新增 `resource-hub/src/app/resources/[id]/page.tsx`，实现 `/resources/[id]` 详情页并展示标题、描述、分类、标签、作者与封面信息。
- 新增 `resource-hub/src/components/resources/resource-detail-interactions.tsx`，实现收藏按钮、下载按钮、评论列表与评论提交表单。
- 详情页接入登录态感知：未登录用户可见引导文案与登录入口，互动操作触发清晰错误提示。
- 评论提交已接入 `POST /api/resources/:id/comments` 并支持提交成功后即时回显；收藏与下载分别接入对应互动 API。
- 在 `resource-hub/src/lib/db/tags.ts` 新增 `listTagsByResource`，用于详情页按资源加载标签数据。
- 增强详情页健壮性：资源查询异常时降级到 `notFound`，避免数据库异常导致页面 500。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass
- 使用 Playwright CLI 访问 `http://localhost:3000/resources`，确认列表页可进入详情路由场景并显示数据异常降级提示: pass
- 使用 Playwright CLI 访问 `http://localhost:3000/resources/demo-id`，确认详情路由存在并返回 404 状态: pass

### Notes:
- 当前 `.env.local` 指向的 Supabase 项目缺少 `public.resources` 表，导致本地无法构造真实资源详情数据进行完整交互演练；已完成代码实现与构建级验证。
- 构建仍会提示 Next.js `middleware` 命名弃用（建议后续迁移到 `proxy`），不影响本任务交付。

## 2026-02-25 - Task: 发布页 UI

### What was done:
- 新增 `resource-hub/src/app/submit/page.tsx`，实现 `/submit` 页面并接入分类、标签数据加载与异常降级提示。
- 新增 `resource-hub/src/components/resources/submit-resource-form.tsx`，实现发布表单（title、description、category、tags）与标签添加/去重交互。
- 发布表单接入 `POST /api/upload`，支持资源文件与封面图片分开上传，并在页面反馈上传状态与错误信息。
- 发布提交流程接入 `POST /api/resources`，提交成功后显示“pending 审核中”提示与资源 ID。
- 扩展 `resource-hub/src/app/api/resources/route.ts`：支持 `tags` 入参、标签去重与数量上限校验，并在资源创建后自动创建标签并写入资源标签关联。
- 新增 `replaceResourceTags` 数据层能力到 `resource-hub/src/lib/db/resources.ts`，用于统一写入 `resource_tags` 关联。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass
- 使用 Playwright CLI 验证 `/submit` 未登录访问会重定向到 `/login?next=%2Fsubmit`: pass
- 使用 Playwright CLI（临时创建并登录已确认测试账号）验证 `/submit` 页面关键控件渲染（标题/描述/分类/标签/上传区/提交按钮）及标签输入交互: pass

### Notes:
- 当前环境分类/标签元数据读取会触发页面级降级提示（`Some metadata failed to load`），但不影响发布表单主流程与构建通过。
- 构建仍会提示 Next.js `middleware` 命名弃用（建议后续迁移到 `proxy`），不影响本任务交付。

## 2026-02-25 - Task: 用户中心 UI

### What was done:
- 新增 `resource-hub/src/app/dashboard/page.tsx`，实现 `/dashboard` 页面并展示用户中心总览（我的资源、收藏数、下载数）。
- 在 Dashboard 中按状态分组展示“我的资源”（`pending/published/rejected`），并补充拒绝原因展示与空状态。
- 新增收藏与下载记录区块，展示历史记录并关联资源元信息，支持快捷跳转到资源详情与再次下载入口。
- 新增 `resource-hub/src/app/dashboard/resources/[id]/edit/page.tsx` 与 `resource-hub/src/components/resources/edit-resource-form.tsx`，提供资源编辑入口与编辑表单（title/description/category/fileUrl/coverUrl）。
- 扩展 `resource-hub/src/lib/db/resources.ts`：新增 `listResourcesByIds`，用于 Dashboard 批量补全收藏/下载记录关联资源信息。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass
- 使用 Playwright MCP 访问 `http://localhost:3000/dashboard`，确认 Dashboard 页面渲染、分组区块与空状态展示: pass
- 使用 Playwright MCP 退出登录后再次访问 `http://localhost:3000/dashboard`，确认重定向到 `http://localhost:3000/login?next=%2Fdashboard`: pass

### Notes:
- 当前环境部分 Supabase 数据读取存在降级提示（页面显示 `Some data is temporarily unavailable`），但不影响 Dashboard 主体结构、编辑入口与构建校验通过。
- 构建仍会提示 Next.js `middleware` 命名弃用（建议后续迁移到 `proxy`），不影响本任务交付。

## 2026-02-25 - Task: 审核后台 UI

### What was done:
- 新增 `resource-hub/src/app/admin/review/page.tsx`，创建 `/admin/review` 审核后台页面并接入管理员权限校验。
- 新增 `resource-hub/src/components/admin/admin-review-panel.tsx`，实现待审核队列展示、资源卡片渲染与审核交互主逻辑。
- 审核卡片支持 `Approve` / `Reject` 操作，`Reject` 强制填写理由并展示逐卡错误反馈。
- 审核动作提交后即时从本地队列移除对应资源，实现“审核后实时更新列表”。
- 增加 `Refresh queue` 按钮，支持主动刷新待审核队列并显示刷新结果反馈。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass
- 使用 Playwright MCP 访问 `http://127.0.0.1:3000/admin/review`，确认未登录会重定向到 `/login?next=%2Fadmin%2Freview`: pass
- 使用 Playwright MCP 临时创建并登录 admin 测试账号后访问 `/admin/review`，确认审核后台页面渲染、队列统计与刷新入口可见: pass

### Notes:
- 当前 Supabase 环境下待审核数据查询与分类元数据查询返回异常，页面会显示降级提示（`Failed to load pending resources. Failed to load category metadata.`）；不影响本任务 UI 结构与审核交互代码交付。
- 构建仍会提示 Next.js `middleware` 命名弃用（建议后续迁移到 `proxy`），不影响本任务交付。

## 2026-02-25 - Task: 错误和加载态

### What was done:
- 新增 `resource-hub/src/app/error.tsx`、`resource-hub/src/app/not-found.tsx` 与 `resource-hub/src/app/loading.tsx`，补齐全局错误页、404 页与应用级加载态。
- 新增 `resource-hub/src/components/skeleton/skeleton-block.tsx`，并在 `resource-hub/src/app/resources/loading.tsx`、`resource-hub/src/app/resources/[id]/loading.tsx` 落地列表页与详情页 Skeleton。
- 新增统一反馈组件 `resource-hub/src/components/feedback/alert-message.tsx` 与 `resource-hub/src/components/feedback/toast-provider.tsx`，并在 `resource-hub/src/app/layout.tsx` 挂载全局 ToastProvider。
- 将资源列表页、资源详情页及详情互动区的散落提示统一为 `AlertMessage`；同步在登录表单和资源编辑表单接入 Toast + Alert 一致反馈样式。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass
- 使用 Playwright MCP 访问 `http://127.0.0.1:3000/resources`，确认列表页错误提示改为统一 Alert 组件并可正常渲染: pass
- 使用 Playwright MCP 访问 `http://127.0.0.1:3000/resources/invalid-id` 与 `http://127.0.0.1:3000/this-page-should-not-exist`，确认 not-found 页面正确渲染: pass

### Notes:
- 构建仍提示 Next.js `middleware` 命名弃用（建议后续迁移到 `proxy`），不影响本任务交付。
- 本地环境缺少完整业务数据时，列表/详情会显示降级提示，但错误与加载态组件均可正常工作。

## 2026-02-25 - Task: 安全与校验

### What was done:
- 新增 `resource-hub/src/lib/validation/schemas.ts`，引入 `zod` 并统一定义写接口校验 schema（资源创建/更新、评论创建、审核动作、上传表单、动态路由 UUID 参数）。
- 改造写接口路由为 schema 校验：`/api/resources`、`/api/resources/[id]`（PATCH/DELETE）、`/api/resources/[id]/comments`、`/api/resources/[id]/favorite`、`/api/resources/[id]/download`、`/api/admin/resources/[id]/review`、`/api/upload`。
- 强化服务端权限边界：在资源详情与编辑/删除中增加 owner/admin 双重校验；资源列表接口对非 admin 的 `pending/rejected` 查询默认收敛为仅本人数据，避免越权查看他人未发布资源。
- 上传接口强化 MIME/扩展名/大小限制：通过 zod 统一校验 `kind`、文件元信息（name/size/mimeType），并返回更明确错误码。
- 评论安全增强：在 `resource-hub/src/lib/db/comments.ts` 增加基础清洗（移除 HTML tag 与控制字符）和长度上限（1000）双重保护。
- 在 `resource-hub/src/lib/auth/admin.ts` 导出 `isAdminUser`，复用统一 admin 角色判定逻辑。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass

### Notes:
- 构建阶段仍提示 Next.js `middleware` 命名弃用（建议后续迁移到 `proxy`），不影响本任务通过。

## 2026-02-25 - Task: 性能与 SEO 优化

### What was done:
- 新增索引迁移 `resource-hub/supabase/migrations/20260225183000_optimize_resource_listing_indexes.sql`，为 `published/latest/hot/downloads` 列表场景补齐组合索引，并补充 `resource_tags(tag_id, resource_id)` 索引以优化标签筛选。
- 在 `resource-hub/src/app/resources/page.tsx` 增加 `revalidate` 与页面级 metadata（description/canonical），并通过 `getCategoryBySlug` 精确解析筛选分类，减少无效查询路径。
- 在 `resource-hub/src/app/resources/[id]/page.tsx` 增加 `generateMetadata`（canonical/Open Graph/Twitter Card）并用 `cache` 复用资源查询，避免详情页与 metadata 重复请求。
- 在 `resource-hub/src/app/page.tsx`、`resource-hub/src/app/resources/page.tsx`、`resource-hub/src/app/resources/[id]/page.tsx` 将封面渲染统一为 `next/image`，补充 `sizes` 与首屏优先策略，优化图片加载性能。
- 在 `resource-hub/src/app/layout.tsx` 与 `resource-hub/next.config.ts` 增强全站 metadataBase/OpenGraph 配置与图片格式优化（AVIF/WebP）。

### Testing:
- 运行 `npm run lint`（in `resource-hub/`）: pass
- 运行 `npm run build`（in `resource-hub/`）: pass
- 运行 Lighthouse（production 模式）:
  - `http://127.0.0.1:3000`：Performance 98 / Accessibility 90 / Best Practices 100 / SEO 100
  - `http://127.0.0.1:3000/resources`：Performance 97 / Accessibility 96 / Best Practices 100 / SEO 100

### Notes:
- Lighthouse 自检显示性能与 SEO 关键指标达标；主要剩余问题集中在部分色彩对比与触控目标尺寸（Accessibility），不阻塞本任务目标。
- 构建阶段仍提示 Next.js `middleware` 命名弃用（建议后续迁移到 `proxy`），不影响本任务通过。
